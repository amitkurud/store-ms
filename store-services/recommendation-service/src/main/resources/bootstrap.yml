app:
  config-server:
    host: localhost
    port: 8888

CONFIG_SERVER_USR: dev-usr
CONFIG_SERVER_PWD: dev-pwd

spring:
  application.name: recommendation
  cloud.config:
    fail-fast: true
    retry:
      initial-interval: 3000
      multiplier: 1.3
      max-interval: 10000
      max-attempts: 20
    uri: http://$***REMOVED***CONFIG_SERVER_USR***REMOVED***:$***REMOVED***CONFIG_SERVER_PWD***REMOVED***@$***REMOVED***app.config-server.host***REMOVED***:$***REMOVED***app.config-server.port***REMOVED***

***REMOVED***
spring.profiles: docker

app:
  config-server:
    host: config-server